# Классификация ландшафтов с использованием нейронных сетей

## Описание проекта

### Постановка задачи

Цель проекта - разработать нейросетевое решение для классификации различных
типов ландшафтов на основе их изображений, при этом отдельные части изображений
заменены не несущими информацию фрагментами. Данная задача имеет практическую
ценность, например, для различных беспилотных систем, от которых требуется
распознавать местность в условиях плохой видимости.

### Данные

В рамках задачи используется датасет ML Olympiad - Landscape Image
Classification. Датасет содержит около 17000 RGB-изображений в формате \*.jpg
размерностью 256 х 256 х 3, разделенных на 6 классов (buildings, forest,
glacier, mountain, sea, street). От 20 до 60 процентов каждого изображения
случайным образом заменены не несущими информацию фрагментами. Для
версионирования данных используется DVC. Сами данные сохранены в публичном
S3-бакете.

Ссылка на страницу Kaggle с данными:

https://www.kaggle.com/competitions/ml-olympiad-landscape-image-classification/data

Примеры изображений из датасета:

<p align="center">
<img src=https://github.com/user-attachments/assets/6a8d575d-f3c5-4feb-9af0-084ab6b0a3ac>
<img src=https://github.com/user-attachments/assets/97ffdcff-e45e-4207-a621-4e2b19362bc4>
<img src=https://github.com/user-attachments/assets/b695ee77-4d47-42a7-a012-36c3c977a227>
</p>

### Метрики

В качестве метрики используется точность (accuracy). Выбор метрики обусловлен
тем, что решаемая задача представляет собой обыкновенную задачу многоклассовой
классификации, при этом данные хорошо сбалансированы, поэтому точность вполне
достоверно отражает качество обучаемых моделей.

### Моделирование

В качестве бейзлайн-модели используется простейшая сверточная нейросеть, которая
имеет арзитектуру вида: сверточный слой - батч-нормализация - функция
активации - макс-пулинг. На выходе бейзлайн-модели установлен адаптивный пулинг
и линейный слой. В качестве основной модели используется предобученная нейросеть
EfficientNet B3.

Обучение всех описанных ниже моделей осуществлялось с помощью библиотек PyTorch
и PyTorch Lightning. Реализация нейросети EfficientNet B3 в PyTorch:

https://docs.pytorch.org/vision/stable/models/generated/torchvision.models.efficientnet_b3.html

Параметры обучения моделей были одинаковыми:

- аугментации: отражение по горизонтали (horizontal flip);
- функция потерь: кросс-энтропия;
- оптимизатор: Adam.

Результаты инференса бейзлайн-модели и основной модели, которые удалось
получить, представлены в таблице:

| Модель          | Loss | Accuracy |
| --------------- | ---- | -------- |
| Бейзлайн        | 0.6  | 0.79     |
| EfficientNet B3 | 0.21 | 0.93     |

P.S. Реализованный пайплан обучения несколько отличается от того, что был описан
в ДЗ 1: в частности, упросилась аугментация данных (из всех аугментаций
сохранился только horizontal flip). Тем не менее полученные метрики полностью
соответствуют исходным ожиданиям.

## Setup и Train

### Структура проекта

```
landscapes-classifier/
├── .dvc/                               # Конфигурация DVC
├── config/                             # Конфигурация Hydra
│   ├── config.yaml                     # Основные настройки
│   ├── logging/logging.yaml            # Настройки логирования
│   ├── data/data.yaml                  # Параметры подготовки данных
│   ├── train_params/train_params.yaml  # Параметры обучения
│   ├── callbacks/callbacks.yaml        # Параметры callback-ов
│   └── model/model.yaml                # Настройки архитектуры моделей
├── data/                               # Данные (необходимо загружать отдельно через DVC)
│   ├── train/
│   ├── val/
│   └── test/
├── plots/                              # Графики функций потерь и метрик
├── landscapes_classifier/              # Основной пакет
│   ├── __init__.py
│   ├── commands.py                     # Точка входа
│   ├── models.py                       # Реализация моделей
│   ├── data_utilities.py               # Функции для подготовки и предобработки данных
│   ├── lightning/
│   │   ├── data_module.py              # Реализация LightningDataModule
│   │   ├── module.py                   # Реализация LightningModule
│   │   └── user_callbacks.py           # Реалзиация callback-ов
│   └── command_fucns/
│       ├── train.py                    # Обучение модели
│       └── infer.py                    # Инференс модели
├── .pre-commit-config.yaml             # Git-хуки
├── pyproject.toml                      # Конфигурация Poetry
├── poetry.lock                         # lock-файл зависимостей
└── README.md                           # Описание проекта
```

### Setup

Алгоритм начала работы с проектом:

1. Клонировать репозиторий:

```
git clone https://github.com/Vladislav-IS/landscapes-classification.git
cd landscapes-classification
```

2. Установить виртуальное окружение:

```
pip install poetry
poetry install
```

3. Установить хуки pre-commit и запустить проверку:

```
poetry run pre-commit install
poetry run pre-commit run -a
```

### Train

Алгоритм обучения моделей:

1. Активировать виртуальное окружение (если оно не было активировано):

```
poetry env activate
```

2. Запустить обучение модели:

```
poetry run python ./landscapes_classification/commands.py train
```

3. Запустить инференс модели на тестовой части датасета:

```
poetry run python ./landscapes_classification/commands.py infer
```

Результаты обучения (чекпоинты, веса модели и т.п.) сохраняются локально.

Параметры обучения (архитектуру модели, пути сохранения логов и т.п.) можно
задавать путем редактирования YAML-файлов в конфигурации Hydra. Назначение
большей части YAML-параметров кажется понятным, однако некоторые параметры,
которые, как мне кажется, надо прокомментировать, перечислены ниже:

- `model.model_name` - позоляет выбрать одну из 2 архитектур для обучения;
- `train_param.norm_mean`, `train_param.norm_std` - среднее и СКО, которые
  используются для нормализации каналов изображений;
- `logging.plots` - папка для сохранения графиков обучения. Графики строятся
  автоматически по завершении обучения.

При запуске `commands.py` из командной строки можно переопределять параметры
конфигурации. В этом случае новые параметры должны иметь вид
`hydra_param=value`. Например, команда:

```
poetry run python .\landscapes_classification\commands.py infer train_params.test_batch_size=128
```

Переопределит размер батча для тестового набора данных.
